# Changelog

All notable changes to UmbraVOX are documented in this file.

The format is based on Keep a Changelog 1.1.0 and this project aims to follow Semantic Versioning.

## [0.1.3] — 2026-05-18

### Assurance Hardening
- ASSURANCE-MATRIX.md: per-module risk table with evidence links and confidence tiers
- ASSUMPTION-GRAPH.md: dependency clusters showing 15 independent roots across 30 assume vals
- REVIEWER-GUIDE.md: tool requirements, reproduction steps, and explicit claims NOT made
- check-assumption-ledger.sh: 6 consistency checks (count, format, cross-ref), fail-closed
- check-proof-hygiene.sh: 4 audit checks with standardized AUDIT NOTE: markers (3-line proximity)
- make assurance-fast: 5-check quick gate (seconds); wired into quality gate
- make assurance: full assurance verification suite

### Fixed
- F* misleading proofs fixed across 12 specs (audit pass)
- Ed25519 encode_decode_round_trip: added on_curve_ext precondition (was false for off-curve points)
- Axiom registry accounting: evidence harness SKIP tracking corrected
- HMAC hmac_prf_assumption renamed to hmac_prf_placeholder (honest naming)
- HMAC/HKDF in-body assumes documented as bounded-path (never reached in practice)
- Makefile admit/Admitted grep patterns fixed to exclude comments

### Added
- Coq Ed25519Prime.v: Pocklington certificate, Fermat witnesses, verified checker (94 Qed, 0 Admitted)
- Coq Ed25519Field.v: field operations and basic properties (54 Qed, 0 Admitted)
- Coq headers hardened with proper module documentation
- Ed25519GroupLaw.v moved to coq/draft/ (has Admitted — proof skeleton only)
- ASSUMPTIONS.md updated with 7 X25519 entries (30 total assume vals)
- Assume val inventory logs with timestamps

### Documentation
- QUICKSTART notes on graceful local fallback when VM image absent
- Assume val count updated to 30, X25519 promotions documented

## [0.1.2] — 2026-05-17

### Fixed
- VM screenshot/record/visual-regression scripts no longer overwrite build cache symlink
- vm-tui-scenario.sh uses `find -L` to follow symlinks into persistent cache
- make run always local (no VM dependency for TUI launch)
- Graceful fallback to local build/test/verify when VM image absent
- VM disk image size increased (2048M headroom for Coq stdlib closure)
- Ed25519 proof fixes: fmul_assoc, pow_one_local, lemma ordering
- Coq Ed25519Constants.v: From Stdlib Require (Rocq 9.1.1 compatible)
- Evidence harness: primality cert timeout + compiled binary detection

### Added
- F* verification inventory logs (assume-val, admit, per-spec summary)
- Infrastructure regression suite: 67 tests (up from 56)
- Coq stdlib in local shell.nix for host-side Coq development
- Screenshot/recording output copied to 9p shared directory

### Documentation
- VM-DEVELOPMENT.md: screenshot/record/visual-regression, network policy, SOCKS5
- F* axiom registry: corrected to 18/24 specs fully proved
- shell.nix banner: 24 F* specs (was 17)

## [0.1.1] — 2026-05-17

### Fixed
- VM shutdown waits for dev-init, network policy grep fix

## [0.1.0] — 2026-05-17

### Formal Verification Milestone
- 0 admit() calls across all 24 F* specs (221 original → 0)
- 23 assume val with complete classification and discharge plans
- 18 of 24 specs fully proved (0 assume val)
- Assumption ledger: test/evidence/formal-proofs/ASSUMPTIONS.md
- External evidence harness: check-external-evidence.sh
- Primality certificate: scripts/primality-certificate.hs
- Coq project skeleton: test/evidence/formal-proofs/coq/

### VM-First Development
- All make build/test/verify route through NixOS VM by default
- VM auto-scales to 50% host CPU/RAM (min 25%, floor 2 cores/2GB)
- Persistent build cache disk — first build ~91s, cached ~6s
- virtio-9p shared output (guest /output/ → host build/vm-output/)
- Infrastructure regression suite: 56 tests (make test-infra)
- Shell identity banners for all 3 environments

## [Unreleased]

### Added
- R1.7.e: tui-sim-chat test coverage for all 5 toolbar buttons (bold, italic, color, link, emoji), insert link dialog (tab cycling, typing, submit, escape), emoji picker (category tab, page navigation, escape), format prompt submission (bold, italic), and rich cursor movement across bold, italic, and link markdown markers.
- M15.1.5: concurrent encrypt thread race test — 20 threads persist unique counter values simultaneously; validates atomic write+fsync+rename prevents corruption.
- M15.3.4-6: SecureBytes mlock/munlock/madvise integration — csrc/secure_mlock.c provides platform-portable memory locking (POSIX mlock, MADV_DONTDUMP/MADV_NOCORE, stubs for Windows/WASM). newSecureBytes now locks pages against swap-out and excludes from core dumps. 6 new tests in Test.Crypto.SecureBytes verify zeroing, round-trip, and mlock integration.
- M15.3.3: IO-boundary key wrapping — KeyStore.hs and Export.hs wrap derived encryption keys in SecureBytes via withSecureKey. Pure crypto function signatures unchanged (GC-heap copy limitation documented).
- M16.1.5: MC/DC condition tables for 5 compound Boolean guards (10 atomic conditions) across AES, Ed25519, ML-KEM, and Keccak modules. Tables in test/evidence/condition-tables/crypto-compound-guards.md.
- M17.3.1: KeyStore disk write guards — saveIdentityKey/saveIdentityKeyAt/saveIdentityKeyWithPassphrase gated by PluginIdentityPersistence. Silent no-op when disabled (ephemeral-by-default).
- M17.5.4: Swap encryption warning — UmbraVox.App.SwapCheck reads /proc/swaps on Linux; warns in status bar when ephemeral mode is active and swap is detected.
- M17.3.6: Startup .pref file guard — persistence preference writes gated by message-storage plugin + ephemeral check.

### Added
- F4.3: SOCKS5 live proxy test — Test.Network.Socks5Live spawns microsocks + echo server on loopback, verifies round-trip through SOCKS5 CONNECT. Closes M6.4 (transport plugin ecosystem).
- VM testing infrastructure: 5 new scripts (vm-tui-scenario.sh, vm-screenshot-capture.sh, vm-record-session.sh, vm-visual-regression.sh, vm-socks5-test.sh) for screenshot capture, video recording, visual regression, and SOCKS5 integration testing inside NixOS VMs.
- Nix: added tmux, aha, asciinema to vm-image.nix; microsocks to vm-test-image.nix and shell.nix.
- Makefile: 5 new targets (vm-socks5-test, vm-screenshot, vm-record, vm-visual-regression, visual-reference-update).

### Fixed
- Handshake: integer overflow in deserializeBundle pqLen arithmetic — validate bounds before offset computation to prevent bypass on crafted packets (Handshake.hs:98).
- RatchetPersist: counter overflow guard — reject rsSendN >= 0xFFFFFFFE before persisting to prevent GCM nonce reuse (RatchetPersist.hs:133). Defense-in-depth alongside DoubleRatchet's own check.
- ConfigFile: verifyConfigHash used isPrefixOf for key filtering, matching unrelated keys like config_hash_pin_old. Now uses exact key match via parseLine (ConfigFile.hs:121).
- Plugin/Registry: resolveEnable silently included phantom dependencies for unknown plugins. Now skips unknown deps instead of injecting them (Registry.hs:147).
- TUI/Dialog: overlayBounds rightW-2 could go negative at small terminal sizes. Now clamped to min 1 (Dialog.hs:82).
- TUI/Render: shortFingerprint on empty ByteString produced empty string. Now pads to 4 bytes (Render.hs:535).
- UDP tests: replaced threadDelay-based synchronization with MVar ready signals to prevent race conditions (UDP.hs).
- Main: --enable-plugin with unknown plugin name now warns to stderr instead of silently ignoring (Main.hs:86).

### Added
- F4.1: UDP transport loopback test suite — 4 tests (round-trip, exact bytes, multi-message ordering, 1KB payload) verifying the seq/ACK reliability layer over localhost.
- M16.1.5: updated MC/DC condition tables with defensive guard analysis — Guards 1, 2, 5 documented as structurally unreachable false paths via private functions (acceptable under DO-178C when documented).
- M17.4.3: --enable-plugin flag wired for runtime effect — resolves dependencies via resolveEnable and enables in runtime plugin registry.
- M17.7.4: config file hash pinning (paranoid mode) — verifyConfigHash computes SHA-256 of config excluding the pin line; set config_hash_pin=<hex> to enable; warns on stderr if mismatch.

### Changed
- TUI: modals now center on the right (chat) pane instead of the full screen.
- M17.7.1: ephemeral mode shows short session fingerprint [abcd1234] in status bar when idle.
- M17.3.7: createDirectoryIfMissing in Startup.hs moved after ephemeral check — no directory creation in ephemeral mode.
- M17.3.2: Anthony SQL operations confirmed guarded at architecture level (cfgAnthonyDB = Nothing in ephemeral mode).

### Fixed
- Handshake: fix crash on empty ByteString in getW32BE when peer disconnects during recv (Handshake.hs:48). Added getW32BESafe with length check; recvBundle and recvInitialMessage now fail cleanly instead of crashing on incomplete length headers.

### Added
- M14.2.2: nix/vm-openbsd.nix — Nix shell environment providing host-side QEMU + SSH tooling for the OpenBSD smoke lane.
- M14.2.3: nix/vm-netbsd.nix — Nix shell environment providing host-side QEMU + SSH tooling for the NetBSD smoke lane.
- M14.2.4: nix/vm-dragonfly.nix — Nix shell environment providing host-side QEMU + SSH tooling for the DragonFlyBSD smoke lane (exits 0 with INFO if GHC binary package unavailable).
- M14.2.2: scripts/vm-openbsd-setup.sh — downloads OpenBSD 7.x amd64 cloud image (or uses cache in build/vm/), boots QEMU, installs GHC + cabal-install via pkg_add, copies source via tar-over-SSH, runs cabal build + test.
- M14.2.3: scripts/vm-netbsd-setup.sh — downloads NetBSD 10.x amd64 cloud image (or uses cache), boots QEMU, installs GHC via pkgin, runs cabal build + test.
- M14.2.4: scripts/vm-dragonfly-setup.sh — downloads DragonFlyBSD 6.x ISO (or uses cache), boots QEMU, attempts pkg install ghc (INFO skip if unavailable), runs cabal build + test if GHC present.
- M14.2.2: make vm-smoke-openbsd — Makefile target invoking vm-openbsd-setup.sh inside nix-shell nix/vm-openbsd.nix.
- M14.2.3: make vm-smoke-netbsd — Makefile target invoking vm-netbsd-setup.sh inside nix-shell nix/vm-netbsd.nix.
- M14.2.4: make vm-smoke-dragonfly — Makefile target invoking vm-dragonfly-setup.sh inside nix-shell nix/vm-dragonfly.nix.
- M14.2.1: nix/vm-freebsd.nix — Nix expression that fetches FreeBSD 14.2-RELEASE amd64 VM qcow2 from the FreeBSD CDN and exposes it as a Nix store path.
- M14.2.5: nix/vm-omnios.nix — Nix expression that fetches OmniOS CE r151052 cloud VMDK and converts it to qcow2 via a Nix derivation.
- M14.5.6: scripts/vm-freebsd-setup.sh — downloads FreeBSD image (or uses cache), builds init ISO with in-guest smoke script, boots QEMU VM, installs GHC + cabal via pkg, runs cabal build + test.
- M14.5.8: scripts/vm-illumos-setup.sh — downloads OmniOS image (or uses cache), converts VMDK, boots QEMU VM, installs GHC via ooce publisher, runs cabal build + test.
- M14.5.6: make vm-smoke-freebsd — Makefile target calling vm-freebsd-setup.sh.
- M14.5.8: make vm-smoke-illumos — Makefile target calling vm-illumos-setup.sh.
- M14.3.1-8: scripts/vm-build-test.sh — universal in-guest POSIX build+test script; works on FreeBSD, OpenBSD, NetBSD, OmniOS/illumos, Linux arm64, DragonFlyBSD; emits VM_BUILD_TEST=PASS/FAIL.
- M14.4.1-5: scripts/release-package-platform.sh — platform release packaging script; takes platform name (freebsd, openbsd, netbsd, illumos, linux-arm64); creates umbravox-{version}-{platform}-{arch}.tar.gz with binary, README, LICENSE, doc/QUICKSTART.md and provenance manifest.
- M14.5.1: make release-freebsd — FreeBSD platform release tarball via nix-shell.
- M14.5.2: make release-openbsd — OpenBSD platform release tarball via nix-shell.
- M14.5.3: make release-netbsd — NetBSD platform release tarball via nix-shell.
- M14.5.4: make release-illumos — illumos/OmniOS platform release tarball via nix-shell.
- M14.5.5: make release-linux-arm64 — Linux arm64 platform release tarball via nix-shell.
- M14.5.9: make vm-smoke-arm64 — Linux arm64 QEMU VM smoke via qemu-system-aarch64; requires arm64 image in build/vm-cache/arm64/.

## [0.0.8] — 2026-05-10

### Testing
- M11 attack test plan: 280 items across 12 categories (SC, PL, SY, AS, KM, IB, PQ, HA, SM, MT, FS, IA).
- M11 Critical tests: 64 tests (side-channel, protocol, symmetric, asymmetric, key management).
- M11 High tests: ~180 tests across 16 modules (protocol, symmetric+hash, key mgmt, forward secrecy, post-quantum, identity/auth, Noise DH vectors, remaining SC/AS/KM/IB).
- M11 Medium tests: ~42 tests covering all remaining medium-priority scenarios.
- Regression test suites: M7 (17), M8 (13), M10 crypto (16), M10 network (15) = 61 regression tests.
- Unicode exhaustive testing: all 1,112,064 code points + surrogates + invalid UTF-8 + all 17 planes.
- All M11 implemented items marked complete in TODO.txt.

### Formal Verification
- M13 F* 100% coverage plan added to TODO (35 items across 6 phases).
- M13 Phase 1 begun: replaced assumes with real Z3-discharged proofs in HMAC, HKDF, Poly1305, GaloisField specs.
- GaloisField: gf_xor_comm, gf_xor_assoc, gf_xor_zero_identity, gf_xor_self_zero fully proved.
- HKDF: counter bounds and ceil_div_bounds proved (removed 2 assumes).
- HMAC: key-length branch pruned (removed 1 assume via type refinement).
- Poly1305: key-size precondition enforced at type level (removed 1 assume).
- SHA-256/512 equivalence lemma stubs + refinement spec added.

### Security
- M10.1.2: Identity keys encrypted at rest with passphrase-derived AES-256-GCM AEAD.
- M10.1.3: Pure-Haskell crypto primitives documented NOT constant-time; compile flag added.
- M10.2.1: PQXDH PQ prekey now Ed25519-signed; verified before KEM encapsulation.
- M10.3.5: DoubleRatchet skipped-key eviction by insertion order (not Map ordering).
- M10.3.7: Storage encryption plaintext passthrough removed (strict UVENC1 enforcement).
- M10.2.16: QEMU/Firecracker runner env vars validated as executable paths (no shell injection).
- M10.3.13: shell.nix pinned to flake.lock nixpkgs revision (reproducible builds).
- M10.3.14: VM agent.env parsed safely (no shell source; key=value allowlist only).
- M10.3.3: Export KDF documented as non-memory-hard with Argon2id migration path.

### Added
- M11 cryptographic attack test plan (280 items across 12 categories).
- M11 critical attack tests: 64 tests covering side-channel, protocol, symmetric, asymmetric, and key management attacks.
- M11 high-priority attack tests: 28 tests covering SHA boundary correctness, HMAC length extension, HKDF edge cases, Ed25519 malleability, X3DH UKS, CBOR bomb, UDP far-ahead.
- M10 regression test suites: RegressionM7 (17 tests), RegressionM8 (13 tests), Regression (16 tests), RegressionNet (15 tests).
- TOFU allowlist in Selective peer trust mode (RuntimeNetwork.hs).
- Per-install random storage encryption salt (Encryption.hs).
- Connection limit (max 64) with per-IP rate tracking on accept loop.
- Message content encryption wired into saveMessage/loadMessages (Anthony.hs).
- Transport socket leak fix via bracketOnError in accept (Transport.hs).
- StealthAddress isValidStealthAddress validator exported.

### Changed
- DoubleRatchet nonce construction: standard 4-zero + 8-byte-LE-counter layout, derived from chain key via separate HKDF label (BREAKING: wire-incompatible with pre-0.0.8).
- DoubleRatchet ratchetEncrypt/ratchetDecrypt return Either RatchetError instead of error/crash.
- Noise IK handshake: fixed se leg to use correct DH (initiator static × responder ephemeral).
- Curve25519 x25519 returns Maybe ByteString (Nothing on all-zero DH output).
- MLKEM bsSlice returns Maybe ByteString on out-of-bounds.
- FStarVerify: --admit_smt_queries false detects assume-holes as failures.
- mDNS: mdnsLastSeen stamped with POSIX time; stale peer eviction.
- Socks5: opaque error messages (no raw status code leakage).
- Comprehensive Finding/Vulnerability/Fix/Verified documentation on all security tests.

### Fixed
- Noise IK se leg duplicated es leg (collapsed mutual auth to one-sided).
- DoubleRatchet skipped-key nonce mismatch (stored chain key alongside msg key).
- FuzzConnection crash on skipMessageKeys with Nothing rsDHRecv.

### Formal Verification
- M13.2.3 Keccak permutation: Spec.Keccak.Permutation.fst now has 0 assumes — pi bounds proved via assert_norm+case-split (pi_table_bounds_lemma), length invariant via Seq.lemma_len_upd, chi_identity via Seq.lemma_seq_of_list_index+case-split, keccak_f1600_preserves_size by type refinement (M13.2.3).
- M13.2.3 Keccak sponge: Spec.Keccak.Sponge.fst now has 0 assumes — absorb_blocks length via arithmetic witnesses (off_q/len_q), state_to_bytes via typed-invariant serialize_state_tail, pad10star1 length via Z3 arithmetic on return type, squeeze_length_lemma proved recursively (M13.2.3).
- M13.2.3 Keccak SHA3: Spec.Keccak.SHA3.fst output-length lemmas (sha3_256/384/512, shake128/256) proved via squeeze_length_lemma; KAT vectors remain as assume (correct) (M13.2.3).
- M13.3.4 ML-KEM-768: Removed 12 false assumes from Spec.MLKEM768.fst — ntt/inv_ntt/basemul bodies no longer re-assume what types already enforce; correctness/KAT Lemma(True) bodies changed to (); parameter correctness checked via assert_norm (k=3, n=256, q=3329, zeta=17) (M13.3.4).
- M13.7.1 Per-module z3rlimit in FStarVerify.hs: Keccak.Permutation 50000, Keccak.Sponge 20000, Keccak.SHA3 20000, MLKEM768 50000; heavySpecs includes all four Keccak/MLKEM modules for cache-warm ordering (M13.7.1).

### Added
- Added provider hot-reload via `reloadProviders` in `ProviderRuntime.hs` that re-scans `providers/*/manifest.uvx` at runtime without restarting (F4.5).
- Added IPC transport hosting (`IPCTransport`, `startIPCProvider`) implementing `TransportHandle` over stdin/stdout with a line-based hex-encoded protocol for `ipc-stdio` providers (F4.5).
- Added IPC provider protocol specification to `doc/transport-providers.md` documenting command/response format, hex encoding, and flush requirements (F4.5).
- Added boundary/edge case differential tests (empty input, pad boundaries, block boundaries, AES roundtrip) (M6.2.4).
- Added HACL* adoption evaluation (`doc/hacl-evaluation.md`) with coverage assessment and correspondence argument (M6.3.5, M6.3.6).
- Added differential test harness (`Test.Crypto.Differential`) comparing C/FFI output against Haskell oracle for all 10 primitives with NIST/RFC test vectors and property-based fuzzing (M6.2).
- Added F* assume reduction plan (`doc/fstar-assume-reduction.md`) categorizing 217 assumes across 17 specs with prioritized reduction strategy (M6.3.2).
- Added real UDP transport with sequence-number reliability layer (F4.1).
- Added SOCKS5 proxy transport for TCP-over-Tor connectivity (F4.3).
- Added transport provider plugin documentation (`doc/transport-providers.md`) covering manifest format, TransportHandle interface, plugin host modes, and provider lifecycle (F4.2).
- Added KaRaMeL/Low* extraction evaluation document (`doc/karamel-evaluation.md`) assessing extractability of all 17 F* specs (F3.1).
- Added arm64 NixOS VM image stub (`nix/vm-image-aarch64.nix`) for cross-architecture testing via QEMU system emulation (M5.5.1).
- Added cross-architecture test matrix documentation in `doc/runner-classes.md` (M5.5.2).
- Added TAP+bridge dual-LAN network setup script (`scripts/vm-network-setup.sh`) for privileged integration testing with pcap capture (M5.3.3).
- Added reproducibility rebuild-and-compare documentation with manual verification steps in `doc/RELEASES.md` (M4.2.3/X2.2).
- Added multi-VM integration test orchestrator (`make vm-integration-test`) with configurable agent count, dgram multicast networking, and parallel QEMU boot.
- Added integration test agent script (`scripts/vm-integration-agent.sh`) with readiness, exchange, and flood scenario support.
- Added PEX auto-connect: received indirect peers trigger best-effort connection attempts via forked threads.
- Added PTY availability check to skip terminal-dependent tests in headless/VM environments.
- Added debugfs dump-based extraction for F* cache files from stage 1 VM (replaces broken grep -oP pattern).
- Added Haskell implementations for all remaining shell-orchestrated targets: release-smoke-linux, release-smoke-appimage, release-lane-qemu, release-lane-firecracker, and release-gate-assurance (M4.3.4).
- Added BSD source-only release policy documentation in doc/RELEASES.md (M3.1.5).
- Added Firecracker VM smoke lane with Haskell orchestration (`make firecracker-smoke`) and NixOS-based vmlinux + rootfs image (`nix build .#firecracker-image`).
- Added Haskell parity tracking for Makefile shell targets with documented migration status for M4.3.4 retirement.
- Added Haskell SBOM generation (`make release-sbom-generate`) listing all dependencies with licenses and sources.
- Added Haskell third-party license bundle generation (`make release-license-bundle-generate`).
- Added Haskell license policy enforcement (`make release-license-check`) with configurable allow-list.
- Added Haskell linking obligations analysis (`make release-linking`) documenting static vs dynamic linking.
- Added Haskell release provenance manifest (`make release-manifest`) with git commit, tag, builder info, and artifact SHA-256 digests.
- Added Haskell release checksum emission (`make release-checksums`) producing SHA256SUMS.txt.
- Added static musl feasibility evaluation documentation (`doc/musl-evaluation.md`).
- Added runner classes and isolation model documentation (`doc/runner-classes.md`) covering host, VM, and container runner tiers.
- Added NixOS QEMU VM image (`nix build .#vm-image`) for fully isolated build/test/release execution with zero external dependencies.
- Added Haskell VM smoke orchestration (`umbravox vm-smoke`) with image caching, source disk creation via genext2fs, and QEMU/KVM boot management.
- Added `make vm-smoke`, `make vm-image-build`, `make vm-image-clean`, and `make image-clean` targets for VM-isolated pipeline execution.
- Added `genext2fs` to nix-shell and flake devShell for non-root source disk image creation.
- Added in-guest smoke pipeline script (`scripts/vm-smoke-run.sh`) that runs build, test, verify, complexity, license, format-check, and release-linux inside the isolated VM.
- Added app-layer AEAD encryption module (`UmbraVox.Storage.Encryption`) providing AES-256-GCM field-level encryption with HKDF-derived per-identity storage keys, migration-safe plaintext passthrough, and deterministic test-key injection.
- Added fail-closed persistence restore behavior: conversations that fail to decrypt are skipped with a `persistence.decrypt.failed` log event instead of crashing the application.
- Added persistence encryption model documentation (`doc/persistence-model.md`) covering encrypted vs. plaintext fields, residual metadata exposure, and rationale.
- Added bounded MVP assurance statement and post-MVP target statement to the assurance roadmap, with reconciled wording across README, AGENTS.md, and the assurance matrix.
- Added persistence confidentiality row to the assurance matrix evidence ledger.
- Added release gate script (`scripts/release-gate-assurance.sh`) requiring the assurance matrix to be present, complete, and not stale relative to crypto source changes.
- Added `make release-gate-assurance` target for assurance matrix freshness checking.
- Added Haskell orchestration subcommands (`runOrchestratedBuild`, `runOrchestratedTest`, `runOrchestratedVerify`) with stable exit-code mapping (0=success, 1=failure, 124=timeout, 127=missing tool) behind `--orchestrated` flag.
- Added runtime logging regression tests (`Test.App.RuntimeLog`) covering redaction completeness, list ordering, permission enforcement, and sensitive-field coverage.
- Added storage encryption tests (`Test.Storage.Encryption`) covering round-trip, migration-safe passthrough, wrong-key rejection, empty/large content, and key derivation determinism.
- Added opt-in `build-haskell`, `test-haskell`, and `verify-haskell` bridge wrappers that keep the legacy `Makefile` path by default while documenting the future Haskell orchestration switch.
- Added explicit active TODO tasks to keep `Makefile` as a thin wrapper while moving build/release orchestration logic into Haskell entrypoints with `nix-shell` workflow parity.
- Added an opt-in Haskell readiness bridge (`make release-lane-readiness-haskell`) that shells out to the existing aggregate readiness script while keeping the shell bridge as the source of truth for now.
- Added a phased release-orchestration migration plan that keeps the current shell bridge honest while tracking the move to Haskell entrypoints.
- Added an experimental AppImage scaffold target plus a non-authoritative smoke placeholder that validates layout only.
- Added consolidated tracking hygiene so completed release-lane and readiness-scaffold work is moved out of `TODO.txt` and only active items remain there.
- Added placeholder compliance gates for future SBOM and license-bundle work (`make release-compliance`, `make release-sbom`, `make release-license-bundle`) that fail clearly when required tools are absent and emit no artifacts yet.
- Added explicit release-target packaging workflow with Linux bundle and source-release targets for Windows CLI, macOS terminal, BSD terminal, and FreeDOS research packaging.
- Added tiered test harness entrypoints (`required`, `core`, `tcp`, `fault`, `recovery`, `soak`, `deferred`) and expanded deterministic suite segmentation.
- Added runtime troubleshooting logging toggles, metadata-focused event emission, and associated regression coverage.
- Added generated/codegen wiring for preserved Haskell wrappers and generated FFI/C bridge participation in active build/test graphs.
- Added transport fallback progress/status reporting and expanded network hardening assertions for fallback endpoint behavior.
- Added release-lane microVM smoke scaffolding for isolated validation of produced Linux artifacts.
- Added documented microVM smoke execution hooks for host-provided QEMU/Firecracker runner commands, plus pinned-input entrypoints for QEMU and Firecracker smoke invocation.
- Added Firecracker pinned-boot smoke documentation as a completed invocation path, while keeping maintained guest-image/config ownership and default in-guest verification out of scope for now.
- Added explicit platform-lane posture and cross-target policy documentation covering native-lane placeholders, source-release targets, and non-authoritative cross-built outputs.
- Added aggregate release-readiness documentation covering host quality gates, Linux smoke coverage, microVM prerequisite checks, and the remaining authoritative-lane gaps.
- Refined release-lane gap output so Firecracker guest-image/config evidence, native runner evidence, and lane-specific next actions point at the exact remaining tasks.
- Tightened the microVM scaffold so pinned QEMU/Firecracker runs require an explicit in-guest verification command template before they proceed.

### Changed
- TUI: fixed pane width split regression to keep left/right box proportions stable across terminal sizes.
- TUI: contact scrollbar now renders only when contact list overflows visible rows.
- TUI: removed empty-state hint text from contacts/chat panes to preserve clean blank-pane behavior.
- TUI: compacted bottom-left action panel height so it no longer matches the full chat input area.
- Updated assurance matrix to reflect real C codegen implementations (no longer link probes) and differential testing framework.
- All 10 generated C files now contain real algorithmic code from .spec files with NIST constants, round functions, and helper macros.
- Restructured TODO.txt with Known Issues (B1), Blocked (X1-X3), and priority tracking sections.
- Switched select Makefile orchestration targets from shell scripts to Haskell entrypoints; shell scripts preserved in scripts/ for reference and packaging targets.
- Marked Makefile as thin wrapper over Haskell orchestration (M4.3.3 complete); all new targets route through Haskell entrypoints.
- Replaced placeholder SBOM/license compliance gates with real Haskell implementations that generate actual artifacts.
- Updated flake.nix to expose vm-image package on x86_64-linux and added QEMU/Firecracker/genext2fs to devTools.
- Expanded runtime logging redaction list from 7 to 15 fields, adding content, answer, key, secret, passphrase, password, token, and session_id.
- Hardened runtime log file permissions to enforce 0600 on every write (not just creation) with single-writer PID tracking.
- Updated Haskell orchestration bridge targets (`build-haskell`, `test-haskell`, `verify-haskell`) to pass `--orchestrated` flag when `UMBRAVOX_USE_HASKELL_ORCH=1` is set, routing to real Haskell implementations instead of shelling back to Make.
- Clarified the AppImage track as scaffold-only and kept it outside the authoritative release posture.
- Reworked messaging MVP hardening flow into explicit fast-gate and targeted suite runs with artifact logging.
- Updated Linux release launch mechanics to use bundled-loader invocation instead of invalid interpreter patching assumptions.
- Improved TUI interaction model with richer simulation coverage for dialogs, menus, browse pagination/search, and runtime status flows.
- Updated release documentation and tracking to reflect the new placeholder SBOM/license compliance gates without overstating artifact generation.
- Clarified release docs to distinguish working Linux container smoke, completed QEMU/Firecracker pinned-boot invocation paths, and the still-missing maintained guest-image/config and authoritative in-guest release execution.
- Updated release tracking to mark only implemented documentation/scaffold tasks complete for platform lanes and cross-target policy, while leaving native execution gaps open.
- Updated quickstart/release lane guidance to state the current aggregate readiness checks and keep unfinished native-lane, parity, and in-guest execution gaps explicit.
- Updated microVM lane guidance to call out the explicit in-guest verification command template now required for pinned runs.

### Fixed
- Fixed host-only peer connect flow to try default port lists with clearer diagnostics and endpoint reporting.
- Fixed non-deterministic test-run behavior by adding explicit fast-gate timeout control and timeout failure reporting in `make test`.
- Fixed restoration/send safety by preventing disconnected restored sessions from being treated as live-sendable peers without reconnect.

### Security
- Added app-layer AEAD storage encryption module for persistent message content and conversation name confidentiality at rest (AES-256-GCM with HKDF-derived keys).
- Strengthened session restore safety boundaries to avoid local loopback/self-delivery risk after persistence restore.
- Tightened logging assurance posture through redaction/scope controls and separation of pre-auth transport arrival vs trusted session establishment semantics.
- Restored full green formal verification gate (17 F* specs) and retained verification as a hard quality gate.

## [0.0.6] - 2026-05-08

### Added
- Tagged release baseline `v0.0.6` and stabilized active MVP hardening branch state.

[Unreleased]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.1.3...HEAD
[0.1.3]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.0.8...v0.1.0
[0.0.8]: https://github.com/UmbraVoxResearch/UmbraVOX/compare/v0.0.6...v0.0.8
[0.0.6]: https://github.com/UmbraVoxResearch/UmbraVOX/releases/tag/v0.0.6
