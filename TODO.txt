UmbraVOX Active TODO
====================
Purpose: active work only. Completed and historical items are tracked in CHANGELOG.txt.

Status: [ ] pending, [~] in progress, [!] blocked
Last updated: 2026-05-08

Rules
-----
- Do not modify `.spec` files, `fstar` files, or primitive crypto implementations without explicit approval.
- Use `nix-shell` for build/test/release commands.
- Keep codegen surfaces wired truthfully; do not overstate assurance claims.
- When a task is complete, remove it from TODO and record the outcome under CHANGELOG.txt (`Unreleased`), then roll into a version section at tag time.

================================================================================
MILESTONE M1: MVP ASSURANCE CLOSEOUT (BLOCKS PUBLICATION)
================================================================================

Goal
----
- Close remaining MVP security and assurance gaps while preserving active functionality.

Exit criteria
-------------
- Persistent storage confidentiality implemented and tested.
- Logging hardening complete and documented.
- Bounded MVP assurance statement and matrix/docs consistency complete.

[~] M1.1 Persistence confidentiality (application-layer, non-SQLCipher claim)
  [ ] M1.1.1 Finalize at-rest model statement (SQLite format unchanged; AEAD at app layer).
  [ ] M1.1.2 Add persistence crypto helper module (reuse existing runtime crypto path).
  [ ] M1.1.3 Encrypt/decrypt message content on save/load with migration-safe handling.
  [ ] M1.1.4 Add deterministic test-key injection path gated as non-production only.
  [ ] M1.1.5 Add DB assertions proving ciphertext-at-rest for protected fields.
  [ ] M1.1.6 Add restart/recovery tests proving decrypted history correctness.
  [ ] M1.1.7 Define fail-closed behavior and user-facing errors for key/ciphertext failure.
  [ ] M1.1.8 Document residual plaintext metadata and rationale.

[~] M1.2 Runtime logging operational hardening
  [ ] M1.2.1 Complete metadata redaction review against current emitted events.
  [ ] M1.2.2 Verify restrictive log permissions and single-writer behavior across modes.
  [ ] M1.2.3 Add regression tests ensuring no sensitive plaintext leaks to logs.
  [ ] M1.2.4 Update docs to mark debug logging as sensitive/non-default telemetry.

[~] M1.3 Assurance publication consistency
  [ ] M1.3.1 Finalize bounded MVP assurance statement (Haskell-first runtime).
  [ ] M1.3.2 Finalize post-MVP stronger target statement (F* spec + Haskell oracle + real C path).
  [ ] M1.3.3 Reconcile README/AGENTS/doc wording with assurance matrix hard limits.
  [ ] M1.3.4 Add release gate requiring matrix update on material assurance changes.

================================================================================
MILESTONE M2: RELEASE ARCHITECTURE (NIX + VM EXECUTION)
================================================================================

Goal
----
- Establish deterministic, reproducible release pipelines with VM-executed authoritative builds.

Exit criteria
-------------
- Pinned Nix release graph exists and produces deterministic artifacts.
- Linux authoritative release runs in Firecracker/QEMU-KVM as defined.
- Native per-host build path remains supported.

[~] M2.1 Nix as release source-of-truth
  [~] M2.1.1 Add `flake.nix` + `flake.lock` with pinned inputs.
    [x] M2.1.1.a Add initial `flake.nix` with devShell + build/test/verify/release app entrypoints.
    [x] M2.1.1.b Generate and commit `flake.lock`.
    [x] M2.1.1.c Document/standardize required flake invocation flags in this environment (`--extra-experimental-features 'nix-command flakes'`).
  [~] M2.1.2 Model build/test/verify/quality/release outputs as Nix derivations/checks.
    [x] M2.1.2.a Add shared check runner and wire build/test/verify/quality checks in `flake.nix`.
    [x] M2.1.2.b Add explicit package outputs for release artifacts (Linux bundle + source releases).
    [x] M2.1.2.c Add deterministic release metadata inputs (tag/commit cleanliness guard paths).
  [x] M2.1.3 Fail release on dirty worktree and non-tagged release attempts.
  [ ] M2.1.4 Keep `nix-shell` developer workflow compatible with new flake outputs.

[~] M2.2 Linux native dynamic bundle hardening (default supported artifact)
  [x] M2.2.1 Replace invalid interpreter patch approach with loader-wrapper launch.
  [ ] M2.2.2 Resolve full transitive runtime closure (not only shallow `ldd` deps).
  [ ] M2.2.3 Add clean-room smoke run in isolated Linux VM/container.
  [ ] M2.2.4 Define and document minimum glibc/ABI compatibility policy.
  [ ] M2.2.5 Add deterministic manifest fields and artifact digest verification.

[ ] M2.3 Linux single-file track (experimental)
  [ ] M2.3.1 Add AppImage target and smoke validation.
  [ ] M2.3.2 Evaluate static musl feasibility and licensing implications.
  [ ] M2.3.3 Keep single-file track experimental until parity and support policy are proven.

[~] M2.4 VM-executed release lanes
  [ ] M2.4.1 QEMU/KVM lane for image bring-up, debug, and compatibility validation.
  [ ] M2.4.2 Firecracker lane as authoritative Linux release executor where practical.
  [ ] M2.4.3 Define runner classes, isolation model, and artifact handoff mounts.
  [ ] M2.4.4 Add smoke microVM lane validating produced Linux artifacts in isolation.

================================================================================
MILESTONE M3: NATIVE PER-HOST BUILD SUPPORT + CROSS-TARGET POLICY
================================================================================

Goal
----
- Keep native builds first-class while using VM lanes for deterministic release production.

[~] M3.1 Platform lanes
  [ ] M3.1.1 Linux x86_64 native runner lane.
  [ ] M3.1.2 Linux arm64 native runner lane.
  [ ] M3.1.3 macOS native runner lane (hosted/native).
  [ ] M3.1.4 Windows native runner lane (CLI).
  [ ] M3.1.5 BSD remains source-only until native builders are available.

[ ] M3.2 Cross-compilation policy
  [ ] M3.2.1 Treat cross-built artifacts as non-authoritative until native parity evidence exists.
  [ ] M3.2.2 Add behavior-parity checks for cross-built vs native-built artifacts.
  [ ] M3.2.3 Publish explicit support matrix by target, builder class, and confidence tier.

================================================================================
MILESTONE M4: RELEASE COMPLIANCE, LICENSE, AND PROVENANCE GATES
================================================================================

Goal
----
- Make release publication contingent on compliance artifacts and policy checks.

[~] M4.1 Licensing and SBOM gates
  [ ] M4.1.1 Generate SBOM per artifact.
  [ ] M4.1.2 Generate third-party license bundle per artifact.
  [ ] M4.1.3 Enforce allow/deny license policy in release pipeline.
  [ ] M4.1.4 Track static vs dynamic linking obligations by target.

[ ] M4.2 Provenance and attestation
  [ ] M4.2.1 Emit checksums + structured manifest (inputs, commit/tag, artifact digests).
  [ ] M4.2.2 Sign manifest/checksums.
  [ ] M4.2.3 Add reproducibility rebuild-and-compare lane for Linux artifacts.
  [ ] M4.2.4 Add attestation path (future: in-toto/SLSA-style) after baseline stability.

================================================================================
FUTURE PLANNED (POST-MVP)
================================================================================

[ ] F1 Promote generated C/FFI from bridge/probe surfaces to semantically real implementations.
[ ] F2 Add large-scale Haskell-vs-C differential testing once C path is semantically real.
[ ] F3 Evaluate KaRaMeL/Low* extraction track as post-MVP refactor.
[ ] F4 Expand transport/provider plugin ecosystem after core release pipeline is stable.
