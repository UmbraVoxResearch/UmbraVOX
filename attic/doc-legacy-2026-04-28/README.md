# Legacy Documentation Archive

This directory is a preserved snapshot of the pre-refresh `doc/` tree moved on
2026-04-28.

## Preservation Scope

The archive includes the prior:

- active `doc/*.md` files
- `future/` planning and research documents
- `hardening/` documents
- `references/` material
- supporting documentation assets such as `BIP39_ENG.txt`

## Purpose

Nothing here was deleted as part of the active documentation refresh. The fresh
active docs now live under `doc/`, while this archive preserves the broader
legacy design and planning material intact for later reuse.
