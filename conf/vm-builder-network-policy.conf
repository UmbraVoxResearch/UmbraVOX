# Network allowlist for the builder VM.
# Only these destinations are reachable from inside the builder VM.
# QEMU restrict=on blocks everything else at the host level.
# Format: ALLOW <domain> <port>

ALLOW cache.nixos.org 443
