-- | 11-day chain truncation + epoch genesis
--
-- See: doc/spec/consensus.md
module UmbraVox.Consensus.Truncation
  ( truncateChain
  ) where

-- | Truncate the chain at the 11-day Universe Cycle boundary.
truncateChain :: IO ()
truncateChain = error "not implemented"
